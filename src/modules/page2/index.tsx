import * as React from "react";
import { render } from "react-dom";
const root = document.getElementById("app");
const load = () =>
    render(
        <>
            <div>
                1111111111
            </div>
        </>,
        root
    );
load();
